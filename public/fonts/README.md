# 字体资源目录

把你的字体文件放到本目录（`public/fonts`）后，页面会自动按 `src/fonts.css` 里的 `@font-face` 名称加载。

## 推荐格式
- `woff2`（优先，体积更小）
- `woff`
- `ttf` / `otf`（也可用，但体积通常更大）

## 约定文件名
文件名需与 `src/fonts.css` 对齐，直接放入即可：

## 注意
- 字体存在授权限制，请确认你有合法使用权。
- 某些字体若未放入本目录，会自动回退到系统字体（不影响页面运行）。
